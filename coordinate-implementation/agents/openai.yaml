interface:
  display_name: "Coordinate Implementation"
  short_description: "Route implementation through a managed worker"
  default_prompt: "Use $coordinate-implementation to stay as coordinator and drive this change through a managed Codex task, using Luna for mechanical work or Terra for routine implementation unless I specify otherwise."
policy:
  allow_implicit_invocation: false
